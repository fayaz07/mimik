## Usage

```html
  <div>
    <AppButton loading={false} content="Small" size="sm" onClick={() => {}} />
    <AppButton loading={false} content="Medium" size="md" onClick={() => {}} />
    <AppButton loading={false} content="Large" size="lg" onClick={() => {}} />
    <AppButton
      loading={false}
      content="Button with Prefix"
      size="md"
      prefix={<span style={{ marginRight: "6px" }}>🚀</span>}
      onClick={() => {}}
    />
  </div>
```
