import keys from "@ed/core/src/lang/keys/Keys";

const genders = [
  { key: keys.gender.male, value: "male" },
  { key: keys.gender.female, value: "female" },
  { key: keys.gender.other, value: "other" },
];

export default genders;
